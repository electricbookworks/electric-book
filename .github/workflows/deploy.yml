name: Build and Deploy

on:
  push: # Trigger on any push, but branch validation will filter
  workflow_dispatch: # Allow manual triggering

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout source repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0 # Need full history for proper git operations

    - name: Load deployment configuration
      run: |
        # Load environment variables from .github/workflows/.env.deploy
        ENV_FILE=".github/workflows/.env.deploy"
        if [ -f "$ENV_FILE" ]; then
          # Export only lines that contain '=' and don't start with '#'
          export $(grep -E '^[^#]*=' "$ENV_FILE" | xargs)
          echo "DEPLOY_BRANCHES=${DEPLOY_BRANCHES}" >> $GITHUB_ENV
          echo "DEPLOY_REPO=${DEPLOY_REPO}" >> $GITHUB_ENV
          echo "DEPLOY_CONFIGS=${DEPLOY_CONFIGS}" >> $GITHUB_ENV
          echo "BUILD_DIRS=${BUILD_DIRS}" >> $GITHUB_ENV
          echo "BUILD_CONFIGS=${BUILD_CONFIGS}" >> $GITHUB_ENV
          echo "✅ Loaded deployment configuration:"
          echo "  Build directories: ${BUILD_DIRS}"
          echo "  Build configs: ${BUILD_CONFIGS}"
          echo "  Allowed branches: ${DEPLOY_BRANCHES}"
          echo "  Target repo: ${DEPLOY_REPO}"
        else
          echo "❌ ERROR: $ENV_FILE file not found"
          exit 1
        fi

    - name: Check if branch is allowed for deployment
      id: check-branch
      run: |
        CURRENT_BRANCH="${{ github.ref_name }}"
        IFS=',' read -ra ALLOWED <<< "${DEPLOY_BRANCHES}"
        BRANCH_ALLOWED=false
        
        for branch in "${ALLOWED[@]}"; do
          if [[ "${CURRENT_BRANCH}" == "${branch// /}" ]]; then
            BRANCH_ALLOWED=true
            break
          fi
        done
        
        if [[ "${BRANCH_ALLOWED}" == "false" ]]; then
          echo "❌ Branch '${CURRENT_BRANCH}' is not in the allowed branches list: ${DEPLOY_BRANCHES}"
          echo "Skipping deployment."
          echo "should_deploy=false" >> $GITHUB_OUTPUT
          exit 0
        fi
        
        echo "✅ Branch '${CURRENT_BRANCH}' is allowed for deployment"
        echo "should_deploy=true" >> $GITHUB_OUTPUT

    - name: Setup Node.js
      if: steps.check-branch.outputs.should_deploy == 'true'
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'

    - name: Setup Ruby
      if: steps.check-branch.outputs.should_deploy == 'true'
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.1'
        bundler-cache: true # runs 'bundle install' and caches gems

    - name: Install dependencies
      if: steps.check-branch.outputs.should_deploy == 'true'
      run: npm run setup

    - name: Build project
      if: steps.check-branch.outputs.should_deploy == 'true'
      run: |
        # Use BUILD_DIRS and BUILD_CONFIGS for builds
        IFS=',' read -ra DIRS <<< "${BUILD_DIRS}"
        IFS=',' read -ra CONFIGS <<< "${BUILD_CONFIGS}"
        
        # Validate that we have the same number of directories and configs
        if [[ ${#DIRS[@]} -ne ${#CONFIGS[@]} ]]; then
          echo "❌ ERROR: Number of BUILD_DIRS (${#DIRS[@]}) does not match number of BUILD_CONFIGS (${#CONFIGS[@]})"
          exit 1
        fi
        
        echo "🔨 Building ${#DIRS[@]} versions of the project..."
        
        # Build each version
        for i in "${!DIRS[@]}"; do
          DIR_NAME="${DIRS[$i]}"
          CONFIG="${CONFIGS[$i]}"
          
          echo "📦 Building version $((i+1))/${#DIRS[@]}: ${DIR_NAME} with config ${CONFIG}"
          
          # Ensure directory name starts with a slash
          if [[ "${DIR_NAME}" != /* ]]; then
            BUILD_DIR="/${DIR_NAME}"
          else
            BUILD_DIR="${DIR_NAME}"
          fi
          
          # Build this version with the corresponding config (prefix with _configs/)
          npm run eb -- output --baseurl="${BUILD_DIR}" --configs="${DEPLOY_CONFIGS},_configs/${CONFIG}" --dontserve=true --deploy=true
          
          echo "✅ Completed build for ${DIR_NAME}"
        done
        
        echo "🎉 All builds completed successfully!"

    - name: Clone target repository and determine target branch
      if: steps.check-branch.outputs.should_deploy == 'true'
      env:
        DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
      run: |
        # Check if token is available
        if [ -z "$DEPLOY_TOKEN" ]; then
          echo "❌ ERROR: DEPLOY_TOKEN secret is not set or is empty"
          echo "Please add a GitHub Personal Access Token as DEPLOY_TOKEN in repository secrets"
          exit 1
        fi
        
        echo "✅ DEPLOY_TOKEN is available"
        
        # Clone with token authentication
        git clone "https://${DEPLOY_TOKEN}@github.com/${DEPLOY_REPO}.git" target-repo
        cd target-repo
        
        SOURCE_BRANCH="${{ github.ref_name }}"
        TARGET_BRANCH="${SOURCE_BRANCH}"
        
        # Check if the source branch exists in target repo
        if git show-ref --verify --quiet refs/remotes/origin/${SOURCE_BRANCH}; then
          echo "Using exact branch match: ${SOURCE_BRANCH}"
          git checkout ${SOURCE_BRANCH}
        else
          # Handle master <-> main mapping
          if [[ "${SOURCE_BRANCH}" == "master" ]] && git show-ref --verify --quiet refs/remotes/origin/main; then
            echo "Source is master, but target repo has main. Using main branch."
            TARGET_BRANCH="main"
            git checkout main
          elif [[ "${SOURCE_BRANCH}" == "main" ]] && git show-ref --verify --quiet refs/remotes/origin/master; then
            echo "Source is main, but target repo has master. Using master branch."
            TARGET_BRANCH="master"
            git checkout master
          else
            echo "Creating new branch: ${SOURCE_BRANCH}"
            git checkout -b ${SOURCE_BRANCH}
          fi
        fi
        
        # Save the target branch name for later steps
        echo "TARGET_BRANCH=${TARGET_BRANCH}" >> $GITHUB_ENV

    - name: Deploy to target repository
      if: steps.check-branch.outputs.should_deploy == 'true'
      env:
        DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
      run: |
        cd target-repo
        
        # Configure git to use the token for authentication
        git config url."https://${DEPLOY_TOKEN}@github.com/".insteadOf "https://github.com/"
        
        # Deploy using BUILD_DIRS
        IFS=',' read -ra DIRS <<< "${BUILD_DIRS}"
        
        echo "📂 Deploying ${#DIRS[@]} directories..."
        
        # Deploy each directory
        for DIR_NAME in "${DIRS[@]}"; do
          # Ensure directory name starts with a slash
          if [[ "${DIR_NAME}" != /* ]]; then
            DEPLOY_DIR="/${DIR_NAME}"
          else
            DEPLOY_DIR="${DIR_NAME}"
          fi
          
          echo "📂 Deploying to directory: ${DEPLOY_DIR}"
          
          # Remove existing folder if it exists
          rm -rf public${DEPLOY_DIR}
          
          # Create the full directory structure for nested paths
          mkdir -p public${DEPLOY_DIR}
          
          # Copy the built site to the public directory
          cp -r ../_site${DEPLOY_DIR}/* public${DEPLOY_DIR}/
          
          # Remove any .gitignore files from the copied content that might prevent tracking
          find public${DEPLOY_DIR} -name ".gitignore" -delete
          
          echo "✅ Deployed ${DEPLOY_DIR}"
        done
        
        echo "🎉 All deployments completed!"
        
        # Configure git
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"
        
        # Add changes and commit (force add to override any gitignore rules)
        git add . --force
        
        # Check if there are any changes to commit
        if git diff --staged --quiet; then
          echo "No changes to deploy"
          exit 0
        fi
        
        git commit -m "Deploy from ${{ github.ref_name }} on ${{ github.repository }}" -m "${{ github.event.head_commit.message }}"
        git push origin ${TARGET_BRANCH}

    - name: Deployment Summary
      if: steps.check-branch.outputs.should_deploy == 'true'
      run: |
        echo "✅ Successfully deployed to ${DEPLOY_REPO}"
        echo "📁 Source branch: ${{ github.ref_name }}"
        echo "📁 Target branch: ${TARGET_BRANCH}"
        echo "📂 Deployed directories: ${BUILD_DIRS}"
        echo "🔧 Build configs used: ${BUILD_CONFIGS}"